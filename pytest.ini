[pytest]
markers =
    smoke: quick end-to-end smoke tests for the Phase 7 grid and summaries

# Prevent recursive test collection in nested clones (fixes "Russian doll" issue)
norecursedirs = hirm-research .* build dist CVS _darcs {arch} *.egg venv
